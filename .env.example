# Client configuration

# URL del server (cambia con l'IP/hostname del tuo server)
SERVER_URL=http://localhost:3000

# Password di uscita dalla modalità kiosk
EXIT_PASSWORD=admin123

# Modalità standalone (se true, non si connette al server e blocca automaticamente)
# Utile per postazioni non in rete o per test
STANDALONE_MODE=false
